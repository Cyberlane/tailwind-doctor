# Tailwind Doctor platform binary

This is a platform-specific binary package installed automatically by
[`tw-doctor`](https://www.npmjs.com/package/tw-doctor). Install the launcher
rather than depending on this package directly:

```bash
npx tw-doctor
```

Tailwind Doctor is not affiliated with or endorsed by Tailwind Labs.
